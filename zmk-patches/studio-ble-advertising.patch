diff --git a/app/include/zmk/studio/rpc.h b/app/include/zmk/studio/rpc.h
index e223b7a..fbfdac5 100644
--- a/app/include/zmk/studio/rpc.h
+++ b/app/include/zmk/studio/rpc.h
@@ -218,6 +218,7 @@ zmk_studio_Response zmk_rpc_subsystem_delegate_to_subs(const struct zmk_rpc_subs
 struct ring_buf *zmk_rpc_get_tx_buf(void);
 struct ring_buf *zmk_rpc_get_rx_buf(void);
 void zmk_rpc_rx_notify(void);
+int zmk_rpc_select_transport(enum zmk_transport transport);

 #define ZMK_RPC_TRANSPORT(name, _transport, _rx_start, _rx_stop, _tx_user_data, _tx_notify)        \
     STRUCT_SECTION_ITERABLE(zmk_rpc_transport, name) = {                                           \
diff --git a/app/src/ble.c b/app/src/ble.c
index 7176bd0..c0ed144 100644
--- a/app/src/ble.c
+++ b/app/src/ble.c
@@ -37,6 +37,10 @@ LOG_MODULE_DECLARE(zmk, CONFIG_ZMK_LOG_LEVEL);
 #include <zmk/event_manager.h>
 #include <zmk/events/ble_active_profile_changed.h>

+#if IS_ENABLED(CONFIG_ZMK_STUDIO_TRANSPORT_BLE)
+#include "studio/uuid.h"
+#endif
+
 #if IS_ENABLED(CONFIG_ZMK_BLE_PASSKEY_ENTRY)
 #include <zmk/events/keycode_state_changed.h>

@@ -78,6 +82,16 @@ static struct bt_data zmk_ble_ad[] = {
                   ),
 };

+#if IS_ENABLED(CONFIG_ZMK_STUDIO_TRANSPORT_BLE)
+static const struct bt_data zmk_ble_studio_sd[] = {
+    BT_DATA_BYTES(BT_DATA_UUID128_SOME, ZMK_STUDIO_BT_SERVICE_UUID),
+};
+
+#define ZMK_BLE_STUDIO_SD_ARGS zmk_ble_studio_sd, ARRAY_SIZE(zmk_ble_studio_sd)
+#else
+#define ZMK_BLE_STUDIO_SD_ARGS NULL, 0
+#endif
+
 #if IS_ENABLED(CONFIG_ZMK_SPLIT_ROLE_CENTRAL)

 static bt_addr_le_t peripheral_addrs[ZMK_SPLIT_BLE_PERIPHERAL_COUNT];
@@ -156,7 +170,8 @@ bool zmk_ble_active_profile_is_connected(void) {
     advertising_status = ZMK_ADV_DIR;

 #define CHECKED_OPEN_ADV()                                                                         \
-    err = bt_le_adv_start(ZMK_ADV_CONN_NAME, zmk_ble_ad, ARRAY_SIZE(zmk_ble_ad), NULL, 0);         \
+    err = bt_le_adv_start(ZMK_ADV_CONN_NAME, zmk_ble_ad, ARRAY_SIZE(zmk_ble_ad),                  \
+                          ZMK_BLE_STUDIO_SD_ARGS);                                                \
     if (err) {                                                                                     \
         LOG_ERR("Advertising failed to start (err %d)", err);                                      \
         return err;                                                                                \
@@ -169,7 +184,9 @@ int update_advertising(void) {
     struct bt_conn *conn;
     enum advertising_type desired_adv = ZMK_ADV_NONE;

-    if (zmk_ble_active_profile_is_open()) {
+    if (IS_ENABLED(CONFIG_ZMK_STUDIO_TRANSPORT_BLE)) {
+        desired_adv = ZMK_ADV_CONN;
+    } else if (zmk_ble_active_profile_is_open()) {
         desired_adv = ZMK_ADV_CONN;
     } else if (!zmk_ble_active_profile_is_connected()) {
         desired_adv = ZMK_ADV_CONN;
@@ -610,13 +627,26 @@ static bool pairing_allowed_for_current_profile(struct bt_conn *conn) {
             bt_addr_le_cmp(zmk_ble_active_profile_addr(), bt_conn_get_dst(conn)) == 0);
 }

+static bool studio_pairing_allowed_without_profile_takeover(struct bt_conn *conn) {
+    struct bt_conn_info info;
+
+    if (!IS_ENABLED(CONFIG_ZMK_STUDIO_TRANSPORT_BLE)) {
+        return false;
+    }
+
+    bt_conn_get_info(conn, &info);
+
+    return info.role == BT_CONN_ROLE_PERIPHERAL && zmk_ble_active_profile_is_connected();
+}
+
 static enum bt_security_err auth_pairing_accept(struct bt_conn *conn,
                                                 const struct bt_conn_pairing_feat *const feat) {
     struct bt_conn_info info;
     bt_conn_get_info(conn, &info);

     LOG_DBG("role %d, open? %s", info.role, zmk_ble_active_profile_is_open() ? "yes" : "no");
-    if (info.role == BT_CONN_ROLE_PERIPHERAL && !pairing_allowed_for_current_profile(conn)) {
+    if (info.role == BT_CONN_ROLE_PERIPHERAL && !pairing_allowed_for_current_profile(conn) &&
+        !studio_pairing_allowed_without_profile_takeover(conn)) {
         LOG_WRN("Rejecting pairing request to taken profile %d", active_profile);
         return BT_SECURITY_ERR_PAIR_NOT_ALLOWED;
     }
@@ -638,6 +668,12 @@ static void auth_pairing_complete(struct bt_conn *conn, bool bonded) {
     }

     if (!pairing_allowed_for_current_profile(conn)) {
+        if (studio_pairing_allowed_without_profile_takeover(conn)) {
+            LOG_DBG("Pairing completed for Studio connection without taking active profile: %s",
+                    addr);
+            return;
+        }
+
         LOG_ERR("Pairing completed but current profile is not open: %s", addr);
         bt_unpair(BT_ID_DEFAULT, dst);
         return;
diff --git a/app/src/studio/gatt_rpc_transport.c b/app/src/studio/gatt_rpc_transport.c
index f0ab315..a5f62f4 100644
--- a/app/src/studio/gatt_rpc_transport.c
+++ b/app/src/studio/gatt_rpc_transport.c
@@ -9,6 +9,7 @@
 #include <sys/types.h>
 #include <zephyr/sys/atomic.h>
 #include <zephyr/kernel.h>
+#include <zephyr/bluetooth/conn.h>
 #include <zephyr/bluetooth/gatt.h>
 #include <zephyr/sys/ring_buffer.h>

@@ -26,6 +27,17 @@ LOG_MODULE_DECLARE(zmk_studio, CONFIG_ZMK_STUDIO_LOG_LEVEL);
 static bool handling_rx = false;

 static atomic_t notify_size;
+static struct bt_conn *studio_conn;
+
+static uint16_t get_notify_size_for_conn(struct bt_conn *conn);
+
+static struct bt_conn *studio_active_conn(void) {
+    if (studio_conn) {
+        return bt_conn_ref(studio_conn);
+    }
+
+    return zmk_ble_active_profile_conn();
+}

 static void rpc_ccc_cfg_changed(const struct bt_gatt_attr *attr, uint16_t value) {
     ARG_UNUSED(attr);
@@ -35,7 +47,7 @@ static void rpc_ccc_cfg_changed(const struct bt_gatt_attr *attr, uint16_t value)
     LOG_INF("RPC Notifications %s", notif_enabled ? "enabled" : "disabled");

 #if CONFIG_ZMK_STUDIO_TRANSPORT_BLE_PREF_LATENCY < CONFIG_BT_PERIPHERAL_PREF_LATENCY
-    struct bt_conn *conn = zmk_ble_active_profile_conn();
+    struct bt_conn *conn = studio_active_conn();
     if (conn) {
         uint8_t latency = notif_enabled ? CONFIG_ZMK_STUDIO_TRANSPORT_BLE_PREF_LATENCY
                                         : CONFIG_BT_PERIPHERAL_PREF_LATENCY;
@@ -62,10 +74,21 @@ static ssize_t read_rpc_resp(struct bt_conn *conn, const struct bt_gatt_attr *at

 static ssize_t write_rpc_req(struct bt_conn *conn, const struct bt_gatt_attr *attr, const void *buf,
                              uint16_t len, uint16_t offset, uint8_t flags) {
-    if (!handling_rx) {
+    int err = zmk_rpc_select_transport(ZMK_TRANSPORT_BLE);
+    if (err < 0 || !handling_rx) {
+        LOG_WRN("Dropping BLE Studio RPC write because BLE transport is not active (%d)", err);
         return len;
     }

+    if (studio_conn != conn) {
+        if (studio_conn) {
+            bt_conn_unref(studio_conn);
+        }
+
+        studio_conn = bt_conn_ref(conn);
+        atomic_set(&notify_size, get_notify_size_for_conn(studio_conn));
+    }
+
     uint32_t copied = 0;
     struct ring_buf *rpc_buf = zmk_rpc_get_rx_buf();
     while (copied < len) {
@@ -89,9 +112,9 @@ BT_GATT_SERVICE_DEFINE(
     rpc_interface, BT_GATT_PRIMARY_SERVICE(BT_UUID_DECLARE_128(ZMK_STUDIO_BT_SERVICE_UUID)),
     BT_GATT_CHARACTERISTIC(BT_UUID_DECLARE_128(ZMK_STUDIO_BT_RPC_CHRC_UUID),
                            BT_GATT_CHRC_WRITE | BT_GATT_CHRC_READ | BT_GATT_CHRC_INDICATE,
-                           BT_GATT_PERM_READ_ENCRYPT | BT_GATT_PERM_WRITE_ENCRYPT, read_rpc_resp,
-                           write_rpc_req, NULL),
-    BT_GATT_CCC(rpc_ccc_cfg_changed, BT_GATT_PERM_READ_ENCRYPT | BT_GATT_PERM_WRITE_ENCRYPT));
+                           BT_GATT_PERM_READ | BT_GATT_PERM_WRITE, read_rpc_resp, write_rpc_req,
+                           NULL),
+    BT_GATT_CCC(rpc_ccc_cfg_changed, BT_GATT_PERM_READ | BT_GATT_PERM_WRITE));

 static uint16_t get_notify_size_for_conn(struct bt_conn *conn) {
     uint16_t notify_size = 23; // Default MTU size unless negotiated higher
@@ -104,7 +127,7 @@ static uint16_t get_notify_size_for_conn(struct bt_conn *conn) {
 }

 static void refresh_notify_size(void) {
-    struct bt_conn *conn = zmk_ble_active_profile_conn();
+    struct bt_conn *conn = studio_active_conn();

     uint16_t ns = get_notify_size_for_conn(conn);
     if (conn) {
@@ -130,7 +153,7 @@ static struct bt_gatt_indicate_params rpc_indicate_params = {
 };

 static void notif_rpc_tx_cb(struct k_work *work) {
-    struct bt_conn *conn = zmk_ble_active_profile_conn();
+    struct bt_conn *conn = studio_active_conn();
     struct ring_buf *tx_buf = zmk_rpc_get_tx_buf();

     if (!conn) {
@@ -206,7 +229,7 @@ static int gatt_rpc_listener(const zmk_event_t *eh) {
     refresh_notify_size();

 #if IS_ENABLED(CONFIG_ZMK_STUDIO_LOCK_ON_DISCONNECT)
-    struct bt_conn *conn = zmk_ble_active_profile_conn();
+    struct bt_conn *conn = studio_active_conn();

     if (!conn) {
         zmk_studio_core_lock();
@@ -220,3 +243,24 @@ static int gatt_rpc_listener(const zmk_event_t *eh) {

 ZMK_LISTENER(gatt_rpc_listener, gatt_rpc_listener);
 ZMK_SUBSCRIPTION(gatt_rpc_listener, zmk_ble_active_profile_changed);
+
+static void disconnected(struct bt_conn *conn, uint8_t reason) {
+    if (studio_conn != conn) {
+        return;
+    }
+
+    bt_conn_unref(studio_conn);
+    studio_conn = NULL;
+    refresh_notify_size();
+}
+
+static struct bt_conn_cb conn_callbacks = {
+    .disconnected = disconnected,
+};
+
+static int gatt_rpc_transport_init(void) {
+    bt_conn_cb_register(&conn_callbacks);
+    return 0;
+}
+
+SYS_INIT(gatt_rpc_transport_init, APPLICATION, CONFIG_APPLICATION_INIT_PRIORITY);
diff --git a/app/src/studio/rpc.c b/app/src/studio/rpc.c
index 7b8dd60..6df6f24 100644
--- a/app/src/studio/rpc.c
+++ b/app/src/studio/rpc.c
@@ -242,13 +242,9 @@ static void rpc_main(void) {
 K_THREAD_DEFINE(studio_rpc_thread, CONFIG_ZMK_STUDIO_RPC_THREAD_STACK_SIZE, rpc_main, NULL, NULL,
                 NULL, K_LOWEST_APPLICATION_THREAD_PRIO, 0, 0);

-static void refresh_selected_transport(void) {
-    enum zmk_transport transport = zmk_endpoints_selected().transport;
-
-    k_mutex_lock(&rpc_transport_mutex, K_FOREVER);
-
+static int select_transport_locked(enum zmk_transport transport) {
     if (selected_transport && selected_transport->transport == transport) {
-        goto exit_refresh;
+        return 0;
     }

     if (selected_transport) {
@@ -273,10 +269,22 @@ static void refresh_selected_transport(void) {

     if (!selected_transport) {
         LOG_WRN("Failed to select a transport!");
+        return -ENODEV;
     }

-exit_refresh:
+    return 0;
+}
+
+int zmk_rpc_select_transport(enum zmk_transport transport) {
+    k_mutex_lock(&rpc_transport_mutex, K_FOREVER);
+    int ret = select_transport_locked(transport);
     k_mutex_unlock(&rpc_transport_mutex);
+
+    return ret;
+}
+
+static void refresh_selected_transport(void) {
+    zmk_rpc_select_transport(zmk_endpoints_selected().transport);
 }

 static int zmk_rpc_init(void) {
